# Add self-loops to a graph

Add self-loops to a graph

## Usage

``` r
gcn_normalize(adj)

adj_row_normalize(adj)

add_graph_self_loops(adj)
```

## Arguments

- adj:

  a sparse COO tensor of the adjacency matrix. Can be weighted.
