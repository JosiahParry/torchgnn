% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer-layernorm.R
\name{layer_layer_norm}
\alias{layer_layer_norm}
\title{Layer Normalization (Ba et al. 2016)}
\usage{
layer_layer_norm(
  in_features,
  eps = 1e-05,
  affine = TRUE,
  mode = c("graph", "node")
)
}
\arguments{
\item{in_features}{Integer. Number of input features per node}

\item{eps}{Numeric. Value added to the denominator for numerical stability.
Default: 1e-5}

\item{affine}{Logical. If TRUE, adds learnable scale and shift parameters.
Default: TRUE}

\item{mode}{Character. Either \code{"graph"} or \code{"node"}. Default: \code{"graph"}}
}
\value{
Tensor \verb{n_nodes x in_features}. Normalized node features
}
\description{
Applies layer normalization to node features:

\deqn{\mathbf{x}' = \frac{\mathbf{x} - \mathrm{E}[\mathbf{x}]}{\sqrt{\mathrm{Var}[\mathbf{x}] + \epsilon}} \odot \gamma + \beta}

The mean and variance are computed over the elements selected by \code{mode}:
\itemize{
\item \code{"graph"}: statistics are computed across all nodes \emph{and} all channels of a
graph, giving a single mean and variance per graph. When \code{batch} is supplied,
each graph in the mini-batch is normalized independently.
\item \code{"node"}: statistics are computed across the channels of each node
independently, giving one mean and variance per node.
}
}
\details{
\code{"graph"} mode removes graph-level location and scale from the representation,
which is what makes it useful for inductive settings where a model trained on
one graph is applied to another. If the new graph's features or degree
distribution sit at a different scale, un-normalized layers propagate that
shift into a systematic bias in the predictions. \code{"node"} mode is the
conventional layer normalization of transformer architectures and does not
depend on the graph partition.

Parameters (when \code{affine = TRUE}):
\itemize{
\item \eqn{\gamma}: \code{in_features} learnable scale, initialized to 1
\item \eqn{\beta}: \code{in_features} learnable shift, initialized to 0
}
}
\section{Forward pass}{

\code{layer(x, batch = NULL, batch_size = NULL)}
\itemize{
\item \code{x}: Tensor \verb{n_nodes x in_features}. Node feature matrix.
\item \code{batch}: Tensor or \code{NULL}. Batch vector assigning each node to a graph,
using 1-based graph indices (e.g. \code{c(1, 1, 2, 2, 2)}). If \code{NULL}, all
nodes are treated as belonging to a single graph. Ignored when
\code{mode = "node"}.
\item \code{batch_size}: Integer or \code{NULL}. Number of graphs. Calculated from
\code{batch} if \code{NULL}.
}
}

\examples{
\dontshow{if (torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
x <- torch::torch_randn(4, 16)

norm <- layer_layer_norm(16)

# Single graph
norm(x)

# Mini-batch of graphs, normalized independently
batch <- torch::torch_tensor(c(1, 1, 2, 2), dtype = torch::torch_long())
norm(x, batch = batch)

# Per-node normalization
norm <- layer_layer_norm(16, mode = "node")
norm(x)
\dontshow{\}) # examplesIf}
}
\references{
Ba, J. L., Kiros, J. R., & Hinton, G. E. (2016). Layer normalization.
\url{doi:10.48550/arXiv.1607.06450}
}
